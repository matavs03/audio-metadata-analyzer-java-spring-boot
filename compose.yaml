services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: audiodb
      POSTGRES_USER: audio
      POSTGRES_PASSWORD: audio
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: